group = org.symly

org.gradle.warning.mode=all

# When true, integration tests are executed in a new JVM
# When false, integration tests are executed in the same JVM
# and environment mocking is required.
opaqueTesting=false