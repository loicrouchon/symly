<project default="compile">
    <target name="compile" depends="clean">
        <mkdir dir="${build-dir.classes}" />
        <javac includeantruntime="false" srcdir="src/main/java" destdir="${build-dir.classes}" release="17">
            <modulepath location="${project.module-path}" />
        </javac>
    </target>
</project>
